# setup.py

from setuptools import setup, find_packages

setup(
    name='shared_python_packages',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # any dependencies the functions might have
    ],
)
